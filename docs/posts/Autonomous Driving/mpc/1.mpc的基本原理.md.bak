---
title: "Model Predictive Control的基本原理"
date:
  created: 2025-06-28
  updated: 2026-06-28
slug: Model Predictive Control的基本原理
categories:
  - Autonomous Driving
tags:
  - Autonomous Driving
  - math

description: "Autonomous Driving"
---

# Model Predictive Control的基本原理



## 1.简单介绍
模型预测控制 (MPC) 是一种控制方法，它在每个控制周期中解决优化问题，以根据给定的车辆模型确定最佳控制序列。计算出的控制输入序列用于控制系统。

<!-- more -->

## 2.车辆运动学模型

| #  | 状态       | 符号        |
|----|------------|-------------|
| 1  | 时间       | \( t \)     |
| 2  | 位置       | \( (x, y) \)|
| 3  | 速度       | \( v \)     |
| 4  | 加速度     | \( a \)     |
| 5  | 角速度     | \( \omega \)|
| 6  | 距离       | \( s \)     |
| 7  | 航向角     | \( \theta \)  |
| 8  | 曲率       | \( k \)     |
| 9  | 前轮转角   | \( \delta \)|
| 10 | 轴距   | \( L \)|


对于MPC来说最重要的当然是模型,对于一个运动系统,往往是使用微分方程来描述它的运动状态.

<figure>
  <img src="image/vehicle_kinematics.png" alt="white" style="width:60%">
  <figcaption style="text-align: center;">运动学模型</figcaption>
</figure>


设一个简易状态量如下:
$$
z = [x, y, v,\theta]
$$
在这个系统中,我关注车辆的位置信息\( x,y \),车辆行驶速度 \( v \),车辆的航向角\( \theta \).
给出微分方程形式:

$$
\dot{z} = \begin{bmatrix}  \dot{x} \\ \dot{y} \\ \dot{v}\\ \dot{\theta} \\ \end{bmatrix}= \begin{bmatrix} vcos(\theta) \\ vsin(\theta) \\ a \\ \frac{vtan(\delta)}{L} \\ \end{bmatrix}
$$

对于控制量,即我希望对外输出的信息来使状态量靠近期望状态量.可设:
$$
u = [a, \delta]
$$
对外输出车辆的加速度与前轮转角
>对于状态量和控制量的选择多种多样,总体上来说,状态量与控制量越多,性能自然就会下降,表现则可能会有提升.
单从横向控制来看位置信息和航向角状态显然是关键状态,添加速度状态可以维持车辆的一个匀速状态,总体上是根据需求进行选择.
比如apollo不仅添加了普通状态量,还添加了相对应的一阶导数,因为apollo使用了方向盘控制的动力模型而不是简单的 \( \theta\).

## 3.线性化和离散化
线性化:MPC需要实时计算,考虑非线性部分过于困难,减轻计算难度
离散化:为了在离散时刻做决策并施加控制量，需要将连续时间模型离散化到相同的采样时刻。从无限维的连续时刻化为有限维的采样点.
上述微分方程经过线性化和离散化才可投入使用.
线性化与离散化的方式也是多种多样,简单一些的离散化可以采用前向欧拉,简单一些的线性化可以直接泰勒展开
>比如apollo就是用的双线性变换 (即 Tustin变换)完成的离散化工作

系统状态方程(前向欧拉+一阶泰勒展开):
$$
z_{k+1}=Az_k+Bu_k +C
$$

A: 状态转移矩阵
$$
\begin{equation*} A  = \begin{bmatrix} 1 & 0 & cos(\bar{\theta})dt & -\bar{v}sin(\bar{\theta})dt\\ 0 & 1 & sin(\bar{\theta})dt & \bar{v}cos(\bar{\theta})dt \\ 0 & 0 & 1 & 0 \\ 0 & 0 &\frac{tan(\bar{\delta})}{L}dt & 1 \\ \end{bmatrix} \end{equation*}
$$
B: 控制输入矩阵
$$
\begin{equation*} B =  \begin{bmatrix} 0 & 0 \\ 0 & 0 \\ dt & 0 \\ 0 & \frac{\bar{v}}{Lcos^2(\bar{\delta})}dt \\ \end{bmatrix} \end{equation*}
$$
C: 偏置方程
$$
\begin{equation*} C =  \begin{bmatrix} \bar{v}sin(\bar{\theta})\bar{\theta}dt\\ -\bar{v}cos(\bar{\theta})\bar{\theta}dt\\ 0\\ -\frac{\bar{v}\bar{\delta}}{Lcos^2(\bar{\delta})}dt\\ \end{bmatrix} \end{equation*}
$$

> 不需要过分关注这些方程,不同的模型产生不同的矩阵


现在我们就可以根据当前的系统状态和一组控制量去预测下一个的系统状态了,也就是模型预测.

## 4.优化问题
### 代价函数
$$
z_{k+1}=Az_k+Bu_k +C
$$
在拥有系统状态方程后,现在要做的就是寻找一组控制量,得到期望的下一时刻状态量.
这就是运筹学中的最优化问题,所以我们首先需要设计一个代价函数:
$$
min\ Q_f(z_{T,ref}-z_{T})^2+Q\Sigma({z_{t,ref}-z_{t}})^2+R\Sigma{u_t}^2+R_d\Sigma({u_{t+1}-u_{t}})^2
$$

其中,\( Q_f \),\( Q \),\( R \),\( R_d \)都是权重矩阵,它们用来设置状态量和控制量的权重.比如在设计中,我更希望车辆能维持一个稳定速度,那我就可能需要设置 \( Q_f \),\( Q \)中速度相关的系数.增大它的参数,让代价函数对速度差更敏感,这样我得到的解中,速度差就会更小.相应的速度变化就更小.

仔细看一下这个代价函数的中间部分:
$$
Q\Sigma({z_{t,ref}-z_{t}})^2+R\Sigma{u_t}^2
$$
稍微解释一下,比如我的预测时域 \( N_p\)是10,控制时域是\( N_c\)也是10.
这意味着在一次计算中,会计算未来10个采样点的状态即\(z_{k+1} ... z_{k+10} \)
$$
Q\Sigma({z_{t,ref}-z_{t}})^2
$$
表示:未来10个采样点,期望状态与预测状态方差的加权和.直观来想,也是预测状态和期望状态越接近越好
$$
R\Sigma{u_t}^2
$$
表示:同上,肯定是希望尽量的少控制就让车辆行驶符合预期

$$
Q_f(z_{T,ref}-z_{T})^2+R_d\Sigma({u_{t+1}-u_{t}})^2
$$
这里的T表示最后一个采样时刻,前式希望整个预测的最后状态与期望状态近一些,后式希望两次控制量之间不要存在突变.

### 约束条件
MPC比LQR强的地方就在这里了.MPC使用二次型求解,因此天然的支持约束条件,而原始LQR使用的求解方法无法添加(当然了,LQR那个是解析解,不能比).
$$
|u_{t+1}-u_{t}|<du_{max} \\
|u_{t}|<u_{max} \\
v_{min} < v_t < v_{max} \\
u_{min} < u_t < u_{max}
$$
主要是对控制量还是速度的限制,这样也取决于模型设计.

## 5.代码实现思路
在尽量删去公式和推导后,还是得到了一堆的矩阵.这些矩阵和公式也就到此为止了,至少代码中它们只能是一个常量,或者根据状态更新的变量.

### 5.1 确定轨迹( \(z_{ref}\))
之前反复提到的期望状态量(\(z_{ref}\)),它究竟是从哪来的呢?
所有的期望状态量都是已知的,不需要进行求解.在mpc开始计算的时候它作为已知量带入.
让我们回顾一下状态量:
$$
z = [x, y, v,\theta]
$$
这样一看就很清楚了,期望状态量就是:
$$
z_{ref} = [x_{ref}, y_{ref}, v_{ref},\theta_{ref}]
$$
有一种说了等于没说的感觉.

横向控制需要轨迹,轨迹由轨迹点构成,轨迹点就是\(x_{ref}, y_{ref}\).
对于轨迹点的切线斜率就是\(\theta_{ref}\).
而速度,可以外部输入比如某些路段有限速,也可以自己指定为车辆期望速度.


用三次样条绘制一个测试路径
<figure>
  <img src="image/20250109-163342.jpg" alt="white" style="width:60%">
  <figcaption style="text-align: center;">轨迹</figcaption>
</figure>

这就是我们的预设轨迹,只需要将它的每个点转化为需要的期望状态量存起来即可.

### 5.2 伪代码
step 1:获取初始/当前车辆状态
step 2:计算参考轨迹(期望状态)
step 3:根据当前状态更新矩阵\( A ,B, C \)
step 4:将参数带入求解器,得出控制量(只取第一个采样点的控制量)
step 5:根据控制量,计算新的车辆状态,即下一个采样点的状态
step 6: go to step1


### 5.3 MPC核心代码

``` python

def linear_mpc_control(xref, xbar, x0, dref):
    """
    线性模型预测控制（MPC）函数
    
    参数:
        xref (np.ndarray): 参考轨迹，形状为 (NX, T+1)，其中 NX 是状态维度，T 是预测时域
        xbar (np.ndarray): 操作点，形状为 (NX, T)，通常是系统的当前状态或最近的状态估计
        x0 (np.ndarray): 初始状态向量，形状为 (NX,)
        dref (np.ndarray): 参考转向角，形状为 (1, T)
    
    返回:
        oa (np.ndarray): 优化后的加速度控制输入，形状为 (NU, T)
        odelta (np.ndarray): 优化后的转向角控制输入，形状为 (NU, T)
        ox (np.ndarray): 优化后的x位置轨迹，形状为 (NX, T+1)
        oy (np.ndarray): 优化后的y位置轨迹，形状为 (NX, T+1)
        oyaw (np.ndarray): 优化后的航向角轨迹，形状为 (NX, T+1)
        ov (np.ndarray): 优化后的速度轨迹，形状为 (NX, T+1)
    """
    
    # 定义优化变量：
    # x: 状态变量，尺寸为 (NX, T+1)，表示从当前时刻到预测时域结束的所有状态
    x = cvxpy.Variable((NX, T + 1))
    # u: 控制输入变量，尺寸为 (NU, T)，表示从当前时刻到预测时域结束的所有控制输入
    u = cvxpy.Variable((NU, T))

    # 初始化成本函数为零
    cost = 0.0
    # 初始化约束列表为空
    constraints = []

    # 遍历每一个时间步，构建成本函数和添加约束
    for t in range(T):
        # 添加控制输入u在时间步t的二次成本，R是控制输入的权重矩阵
        cost += cvxpy.quad_form(u[:, t], R)

        # 如果不是初始时间步，添加状态误差的二次成本，Q是状态误差的权重矩阵
        if t != 0:
            cost += cvxpy.quad_form(xref[:, t] - x[:, t], Q)

        # 获取线性化后的系统矩阵A, B, C，基于操作点xbar和参考转向角dref
        A, B, C = get_linear_model_matrix(
            xbar[2, t],  # 操作点的速度（xbar的第3维是速度）
            xbar[3, t],  # 操作点的航向角（xbar的第4维是航向角）
            dref[0, t]   # 参考转向角
        )
        # 添加系统动态约束：x_{t+1} = A*x_t + B*u_t + C
        constraints += [x[:, t + 1] == A @ x[:, t] + B @ u[:, t] + C]

        # 如果不是最后一个时间步，添加控制输入变化的成本和约束
        if t < (T - 1):
            # 添加控制输入变化的二次成本，Rd是控制输入变化的权重矩阵
            cost += cvxpy.quad_form(u[:, t + 1] - u[:, t], Rd)
            # 添加转向角变化的约束，限制转向角的变化率不超过最大允许值
            constraints += [cvxpy.abs(u[1, t + 1] - u[1, t]) <= MAX_DSTEER * DT]

    # 添加终端状态误差的二次成本，Qf是终端状态误差的权重矩阵
    cost += cvxpy.quad_form(xref[:, T] - x[:, T], Qf)

    # 添加初始状态约束，确保优化的第一个状态等于给定的初始状态x0
    constraints += [x[:, 0] == x0]

    # 添加状态速度的上限约束，假设状态变量的第3维是速度
    constraints += [x[2, :] <= MAX_SPEED]
    # 添加状态速度的下限约束
    constraints += [x[2, :] >= MIN_SPEED]

    # 添加加速度控制输入的绝对值约束，限制加速度输入不超过最大加速度
    constraints += [cvxpy.abs(u[0, :]) <= MAX_ACCEL]
    # 添加转向控制输入的绝对值约束，限制转向输入不超过最大转向角
    constraints += [cvxpy.abs(u[1, :]) <= MAX_STEER]

    # 打印信息，表示开始求解MPC优化问题
    print("开始求解MPC优化问题")
    # 构建优化问题，目标是最小化成本函数，满足所有约束
    prob = cvxpy.Problem(cvxpy.Minimize(cost), constraints)
    # 求解优化问题，使用指定的求解器（CLARABEL）
    prob.solve(solver=cvxpy.CLARABEL, verbose=False)
    # 打印信息，表示优化问题已求解完成
    print("MPC优化问题求解完成")

    # 检查优化问题的求解状态
    if prob.status == cvxpy.OPTIMAL or prob.status == cvxpy.OPTIMAL_INACCURATE:
        # 如果求解成功，打印成功信息
        print("MPC求解成功，获取优化控制输入")
        # 从优化结果中提取状态变量的值，并转换为NumPy数组
        ox = get_nparray_from_matrix(x.value[0, :])      # x位置轨迹
        oy = get_nparray_from_matrix(x.value[1, :])      # y位置轨迹
        ov = get_nparray_from_matrix(x.value[2, :])      # 速度轨迹
        oyaw = get_nparray_from_matrix(x.value[3, :])    # 航向角轨迹
        # 从优化结果中提取控制输入的值，并转换为NumPy数组
        oa = get_nparray_from_matrix(u.value[0, :])      # 加速度控制输入
        odelta = get_nparray_from_matrix(u.value[1, :])  # 转向角控制输入
    else:
        # 如果求解失败，打印错误信息
        print("错误: 无法求解MPC问题")
        # 将所有返回值设置为None
        oa, odelta, ox, oy, oyaw, ov = None, None, None, None, None, None

    # 返回优化得到的控制输入和状态轨迹
    return oa, odelta, ox, oy, oyaw, ov
```

> apollo调用的是OSQP,OSQP本身就有迭代效果,如果是使用常规求解器,通常还会多算几次防止误差过大.

这里可以看到代码和之前推导时产生的矩阵公式一一对应