---
title: "入门笔记(来时的路) - C++ 笔记"
date:
  created: 2023-02-05
  updated: 2023-04-12
slug: beginner-cpp-notes
categories:
  - Archive
tags:
  - C++
  - Beginner Notes
description: "这是我入门时的草稿笔记"
draft: true
---

<!-- more -->

1.顶层const 底层const
顶层const:指针本身是个常量不能改变指向,底层const 指针指向一个常量
普通数据结构只需要考虑const,对于普通数据  就是表示不能修改

对于引用,所有的const都代表顶层const  即不能改变引用自身的指向
 
const int * p=a;  不能通过p修改a
int * const p=a;  p里面指向的地址不能改变

2.auto  迭代器 for

for(auto i=vec)
	for(auto &j=i)
	
第二个的&是必须加的  因为编译器会转化auto操作中的数组头

编译器不会转化数组为指针的情况只有如下四种情况 p143
decltype
sizeof
& 取地址
typeid

3.能用++i  就不用i++
++和-- 必须存在的原因是因为某学迭代器不支持算数运算
++i比i++节省性能 因为不用保存原始值

4. a+=1比 a=a+1 更节省
a少运算一次

5.decltype

decltype((****))  如果里面带括号  则不管什么类型都是引用
decltype(*p)    得到的一定是引用类型,因为*p暗含可操作指向对象

6.引用

double a=3.14
const int &r=a;      正确
int &r=a;            错误

因为当涉及转化的时候,为了确保可以引用到指定变量,
编译器会增加一个临时变量,这个临时变量是肯定不能修改的 所有必须加上const
对于引用所有的const都是底层const 不能通过引用修改原值

7,强制类型转化
static_cast<int>(a)

这个最适合从高精度转到低精度上面  相当于告诉编译器 不需要精度部分
这个也能让 void* 进行转化

const_cast  函数重载的时候使用

还有几种就不说了,总之出来  函数重载的那个类型转化,其他的强制类型转化的能不用就不用

8. 函数传参建议使用引用,如果函数中不会对参数进行修改就增加const修饰符

9.尾置返回  好东西

auto func(int i)->int (*)[10]

int (*func(int i))[10]

以上两者等价  函数 入参为int  返回一个指针   指向一个长度为10 的int数组

10.函数增加static修饰标明此函数只在此cpp内有用,无法跨文件
静态函数适用于小的简单 函数,直接在cpp声明并且使用
它不会被其他文件识别到

变量：众所周知，static变量只能在定义它的文件中使用。这里的文件指的是cpp源文件。如果在头文件中定义了static变量，那么，所有包含这个头文件的源文件都会定义自己的static变量，而不是使用该头文件中的static变量。所以也就造成了，在头文件定义static变量，其他包含头文件的源文件也能使用该变量的假象。

函数：与static变量相同，源文件中的static函数只供源文件使用，而头文件中的static函数则会供所有包含它的源文件使用。

总结：只有在源文件中使用static关键字，其修饰的变量或函数被其他文件调用时才会出现未声明的状况。一定要避免在头文件中使用static。

另：https://ask.csdn.net/questions/177352中的回答：

static限定函数的时候，和普通函数没有很大的区别的。这里说的函数不是成员函数，而且定义在头文件中！
唯一的区别就是，在调用该函数的时候，static函数在内存中只维持一份，而普通函数每次调用都会维持一份拷贝。
当然这个静态函数也可以在其他文件中调用了！只是要包含它所在的头文件了。

经测试是正确的。

11.定义在类内部的函数都是隐式的inline
12.  无参构造函数不能显式调用
13.左值右值
有这么几个定义
最老版本:赋值号左边是左值,右边是右值
c++11 :   有地址不可移动 是左值 
               有地址可移动是xvalue但是地址不可访问
              无地址可移动  是纯右值

	xvalue既是左值又是右值

为什么要这么分,因为有寄存器一档子事
寄存器上面的就是纯右值,函数返回值一般也是右值,
想string.c_str()就是xvalue

如果是在寄存器上面的东西右值引用会在内存上面建立临时变量   是的  这个右值引用自己就是纯左值
如果是内存上面的右值,也就是xvalue那就是一个浅拷贝

警惕 	char *(&& kr)= (char *)(string("abc").c_str());
它确实的右值引用,但是(char *)(string("abc").c_str());会在语句结束被析构

stl move  它会把左值变成右值,这样函数重载的时候可以百分比匹配到右值上面
一旦使用移动构造函数,原本对象里面的指针得置空,因为现在是移动
forward  完美转发  它可以让转发的时候保持值性质不变,右值一直为右值 左值依然是左值

15.小知识 string.c_str() 析构就是直接把字符串头变成00
而且这玩意受string自己的生命周期影响
string("abc")就只能维持到语句结束
str就能支持到str自己的生命周期

16.类成员  初始化和构造函数赋值不同
const成员必须通过初始化完成或者通过构造函数初始值列表(推荐)

17.如果调用无参构造函数(无论默认还是自己生成的)
A a;		正确
A a();      错误  这会被视为一个函数声明

18.强制去除括号内字符串转义  	R"(D:\Desktop\io.txt)" 解决斜杠问题
"

19.vector.begin()  这个东西有两个版本,一个返回的是iterator  另外一个返回const iterator
如果直接auto  返回的是前者  现在新版本出来了cbegin  auto可以直接获得const

20 explicit
构造函数defalut和delete
constexpr  
引用
const

这几个哥们,能用就用

21
由于array不能扩容，要比较的话也要放到同一个水平线做比较，因此这里我们默认vector：1.初始化后马上resize到合适的大小。2.随后的操作不改变vector的容量。在这种情况下，vector和array的性能主要差距在于：vector依赖动态分配/释放内存，这是比较耗时的。array的内存大小固定，编译时直接分配一段栈上/静态存储区内存即可。速度远快于vector。因此：1.假如你的程序需要频繁的创建销毁容器，大小固定（而且不会爆栈），则选用array较为合适。2.如果创建销毁不是特别频繁，且容器大小不确定，可以选用vector。3.正确使用下的vector，和array的性能差距在大部分情况下不会构成瓶颈。


22.类内初始值和赋值

一般来说问题不大.但是如果类内成员是const.或者引用的时候,就必须要在构造函数的括号里面进行初始化

初始化的顺序也是按照 类内成员出现在类内的顺序

23.原则上建议swap的非成员版本

24.5.在C++中，上面的ecx的值即为this指针
this指针特点如下：

你用或者不用，它就在那里
参数个数确定的时候，用ecx来传递
参数个数不确定的时候，最后一个传递(参见不定长参数)
this指针不能做++ – 等运算，不能重新被赋值.
this指针不占用结构体的宽度.


25.为什么说动态库显示调用没有lib隐式的好
因为c++编译的时候会修改函数名称,lib则包含全部的符号
隐式的缺点是动态库位置无法改变,但这个无伤大雅

关于函数调用参见  需要解决的问题.txt

26 继承构造函数

子类的优先级要大于父类
父类如果有两个构造函数,子类里面为空,就写个继承构造
Sub s1(3,2); 子类可以直接写成这样  
但是不等效于  直接在子类里面定义一个一模一样的构造函数

比如Sub(int v,int p) 如果这个东西是继承构造函数搞出来的  子类根本没定义
那它只会调用一次Base(int a,int b)

假如你有继承构造,子类里面也定义了一个一样 的东西,则它会按照通用规则
先调用父类的无参构造函数,再调用子类的sub(int,int)根本不会调用到base(int,int)
也会多掉一次父类无参构造

继承构造函数具备传递性  规则与上面相同

27 局部静态变量可以保证创建时 线程安全

28.错误 1  error LNK2005: "public: static int c1::i" (?i@c1@@2HA) 已经在 c1.obj 中定义
错误 2  error LNK1169: 找到一个或多个多重定义的符号 

只要两个或者以上的.cpp同时包含了一个定义了变量的.h文件，就会提示错误.

在网上查资料以后，在这里找到了答案，http://akheyun.blog.163.com/blog/static/138249276201062221452697/

有1.cpp和2.cpp, 1.cpp包含1.h, 2.cpp只包含2.h 但是2.h包含了1.h.. 所以1.h中定义的变量编译的时候会在2.cpp中有定义. 于是乎2.cpp翻译成的obj里也会有定义, 结果导致提示obj里重定义。

因此，我们要做的就是，不要在.h里面初始化静态成员变量，初始化静态变量的工作放到.cpp里面去完成
