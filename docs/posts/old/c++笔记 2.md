---
title: "入门笔记(来时的路) - C++ 笔记 2"
date:
  created: 2023-04-18
  updated: 2023-06-03
slug: beginner-cpp-notes-2
categories:
  - Archive
tags:
  - C++
  - Beginner Notes
description: "这是我入门时的草稿笔记"
draft: true
---

<!-- more -->

1.forward_list 单向列表没有size操作, 追求极限速度    其他容器的size操作,保证是一个常量时间
2.forward_list  和list更占用内存
3.最佳实践,如果不确定使用何种容器,则应该使用vec和list 的公共操作,使用迭代器,不用下标,避免随机访问.这样切换起来很方便
4.forward_list  不支持--  很合理,人家是追求极致性能的单列表
5.全局变量(外部变量)的说明之前再冠以static 就构成了静态的全局变量。
全局变量本身就是静态存储方式， 静态全局变量当然也是静态存储方式。 这两者在存储方式上并无不同。
这两者的区别在于非静态全局变量的作用域是整个源程序， 当一个源程序由多个源文件组成时，非静态的全局变量在各个源文件中都是有效的。 而静态全局变量则限制了其作用域， 即只在定义该变量的源文件内有效， 在同一源程序的其它源文件中不能使用它。由于静态全局变量的作用域局限于一个源文件内，只能为该源文件内的函数公用，因此可以避免在其它源文件中引起错误。 static全局变量只初使化一次，防止在其他文件单元中被引用; 　 

6.支持随机访问的容器  插入后都会导致迭代器指针引用失效   但是返回的是有效迭代器
7如果一个迭代器指向  0 1 2 3  中的2  在对他执行插入后  容器变为  0 1 new 2 3
迭代器会指向new  相当于新容器的2

如果对2删除 容器变为  0 1 3  迭代器指向 3 也相当于新数组的2  如果只有一个元素  删除之后变成end

8.对 emplace_back 和push_back 都将创建新的对象,但是 emplace是在容器管理的内存中创建对象,而push会创建一个局部临时对象,并将其压入

从某种意义上  emplace 完美替代push

9.vec.at(n) 比 vec[n]强  因为at()  如果超界了会有稳定报错  所有的访问成员函数返回的都是引用
其实没鸟用,因为我不用trycatch(20220528   被打脸了,俺最爱异常了,异常太香了)
10,forward_list的特殊操作
因为它是单链表  而删除操作不可避免的会影响前驱,而单链表是无法简单访问前驱,它只能很简单访问后驱,所以给它准备了特殊版本的删除
操作 1 2 3 4 5 6
如果想删除3  则迭代器要放在2处 删除之后 返回也是2

单链表存在首前操作,上面的三种操作 不可以把end()作为入参,因为它不是在当前位置操作,而是作用当前位置之后,end已经是尾后迭代器了

如果存在forward_list的遍历操作  记得从首前节点开始
9.1 看书339页笔记  讲accumulate 转换的

10 lamda表达式
	size_t sz = 2;
	auto f = [sz]()   mutable  {return ++sz; };
	auto k= f();

	log(k);
	log(sz);
	k = f();
	log(k);
	log(sz);
 输出结果:
70:containerMain():   k=[3]
71:containerMain():   sz=[2]
73:containerMain():   k=[4]
74:containerMain():   sz=[2]


表达式中的值拷贝被永久的改变了,但是外面不变
(现在lamdba已经彻底学懂了)

11.如果想实现find_if 多元谓词操作
有三种方式  
1.lamba表达式
2.bind  记得占位符 _1需要添加命名空间
3.写一个类  重载这个类的括号运算符  因为 find_if就是这样实现的
https://blog.csdn.net/xinzhi111/article/details/123296887

12 反向迭代器和正向迭代器 通过reverse_iterator是转换是不对称的
反向转正向 指向的内容会退后一位

13remove_copy_if函数与remove_if函数的唯一不同的地方在于：将最后的结果保存在一个容器中，原容器的值不变。

14 常用泛型算法
merge  将两个容器合并

15 内联函数
不讨论类成员函数，只说全局函数的话想定义在.h里，前面必须加static 或者 inline。本来说这个感觉是常识，但是怕被人打脸，
还是细说一下吧：A.h 文件里定义一个函数 void foo(){}，分别被 B.cpp和C.cpp引用，然后把B、C各自对应的头文件 B.h和C.h include到main.cpp里，
调用B、C里用到 foo()的函数。这时候应该会有链接错误，提示foo重定义了。然后给foo前面加inline 或 static 可以使之编译通过。
inline自不必说是内联，static全局函数其实是不建议放在.h里的（网上有相关的讨论），
但是能编译通过，原理是static让这个函数在每个被用到的.o里都生成一个自定义的名字，
比如在B.cpp里它叫B_foo，而是C.cpp里它叫C_foo，相当于foo在每个.o里各自编译了一份，
其实跟inline差不多，跟inline区别在于，假如B.cpp里好多地方用到foo，inline相当于同一份代码拷贝好多次。而static，
是在同一个.cpp里只生成一份编译出来的代码，还是要比inline省一些空间的。

16.自定义规则的map
multimap<sp, int, decltype(compareMap)*> map1(compareMap);

其实sp为自定结构  不重载<号  所以需要外部指定比较函数  decltype(compareMap)* 为函数指针
如果重载了<就不用写第三个参数了

如果同时重载了<号并且写了比较函数,优先比较函数

17  auto 作为函数返回值,似乎不能跨文件  未解之谜
查了下stackoverflow  似乎是不行的   
好像是得用模板写才能放在头文件里面这样用

18.c++17 智能指针 见我自己的练习项目  smartPointerMain
在C ++17标准之前，您不能拥有std::shared_ptr<T[]>指针。但即使在C++17中，
std::make_shared函数也不支持数组类型，因此您必须使用boost::make_shared。
另一种方法是将唯一指针与支持数组类型的std::make_unique结合使用。如史考迈尔斯在他的“有效性”中指出的那样，这可能不是一个好主意 现代C ++“书：

数组的std :: unique_ptr的存在应该是唯一的   对你有兴趣，因为std :: array，std :: vector和   std :: string几乎总是比数据结构选择更好   原始数组。
为什么需要动态申请内存
三个场景
1.程序不知道自己需要使用多少对象
2.程序不知道对象的准确类型
3程序需要在多个对象间共享内存

19.unique_ptr的特别之处
1，unique_ptr默认删除了复制构造函数，所以在使用智能指针过程中一旦无意中用到了其复制构造函数，便会出现C2280的错误，显示引用已经删除的函数。

2，标准容器中存放的元素为unique_ptr时，应该返回为这个容器的引用，且最好是常引用，若是直接返回整个容器，便会调用复制构造函数；当vector使用resizer扩容时，会将对应的容器元素默认构造，那么std::unique_ptr默认构造会产生一个为NULL的指针，智能指针便不再生效，在使用前一定要初始化，出现尝试引用已删除的函数。

3，返回unique_ptr时，可用std::move转换，若是直接返回局部构建的unique_ptr便是调用默认构造函数。std::move并不能移动任何东西，它唯一的功能是将一个左值强制转化为右值引用，继而可以通过右值引用使用该值，以用于移动语义。std::move是将对象的状态或者所有权从一个对象转移到另一个对象，只是转移，没有内存的搬迁或者内存拷贝所以可以提高利用效率,改善性能.。

4，返回整个unique_ptr的容器时，是一层浅拷贝，只是返回了容器的地址，但容器内的智能指针没有复制构造函数，所以是共用的一块地址，当源容器销毁时，就会出现错误

5.resize这个方法很特别
resize(n);则是直接调用n次无参构造函数
resize(n,obj)  直接调用n次拷贝构造  但是它的拷贝构造是从vec[0]为根本
resize(n,t) 先调用1次带参构造函数,再调用n次拷贝构造函数

20 一个基本原则,当用shared_ptr绑定普通指针时,原则上就不应该再次使用内置指针去访问shared_ptr指向的内存

21.shared_ptr和unique_ptr都有自己的专用的释放的方式
但是两者存在区别
shared重载了构造函数,只需要在参数列表里面写明函数即可,删除器应当接受指针类型

unique_ptr需要在模板列表里面加上 decltype(func)*

22  _ptr和make的区别在于  目标堆和引用计数是否连续

23  io缓冲区位置
位于堆区的，这么说吧，栈区的内存是编译时可确定的。才会在栈区分配内存。io流读进来的数据量没办法确定 所以只能在堆区分配空间去存
-----------------------完善答案----------------------
感谢同学在我答案的评论里面提出关于缓冲区问题的质疑，我又去查了资料深入了解了一下。发现我上面的答案是错误的，完全错误的，我来更正一下。
缓冲区的位置是不确定的，不确定是在堆区还是在栈区。我之前的回答混淆了一个概念，作为警示，就不删除了。
举个读取文件的例子说明问题。在读取文件的时候，使用的是ifstream这个类。但是这个类里面有一个缓冲区。是streambuf类型的。这个缓冲区是可以人为设置的。例如下面这样一个例子

根据cppreference站点，设置pubsetbuf的顺序很重要，因为需要在打开任何文件之前对其进行设置，否则它将无效。因此，这就是代码的顺序（根据我的情况）
int sz {131072};          // buffer size
std::vector<char> buf;   // std::vector instead of C-style char
buf.resize(sz);
std::ofstream outf;      // declaration, only
outf.rdbuf()->pubsetbuf(&buf[0], sz);  // set buffer before...
outf.open("file.txt");                 // ...opening the file
// rest of the code



ifstream file("a.txt");
char buf[256] = "buffer";
streambuf * pBuf = file.rdbuf();
pBuf->pubsetbuf(buf, 256);//这里设置缓冲区

上面这段代码的缓冲区就是在栈上面，如果把其中的char buf[256]改成动态new出来的空间，那么缓冲区就是在堆上面了。这才是真相，对于一开始没有深入研究就武断的下结论实在抱歉。

24  为什么赋值运算符必须是成员函数
1.当class中没有重载赋值操作符时，class本身会合成一个赋值操作符 倒过来看，class需要知道自己是否应该合成一个赋值操作符，所以要把赋值操作符定义为成员函数 
2.类的成员函数都有一个隐含的形参，叫做this，它指向当前类对象

不使用成员的成员函数的this指针会在release情况下被去掉 但是debug不会

至于为什么运算符重载必须按照这个需求来做,我也不知道了

25
	//拷贝构造
	Mess mes3( mes2);

	//赋值运算符重载
	mes1 = mes2;

	//单纯的构造
	//这个肯定被编译器优化了
	Mess mes4=Mess ("mes2");

	//拷贝构造
	Mess mes5 = mes2;

26. 成员包括函数和数据两部分
私有成员函数也是不能被子类看到的

27 static_cast
是在已知安全的情况下进行类型转化
强制覆盖编译器操作

28  p544 
当d公有的继承b时,用户代码才能使用派生类向基类转换
原因:
派生类的作用域是嵌套在基类里面的   其中作用域的范围被访问符控制  当然这个访问符不会阻碍派生类成员和友元访问
当在外部的时候,因为私有继承,外部用户无法通过派生类作用域访问到基类的成员  指针指向过去之后,就会导致超作用域访问
说到底还是作用域问题

29
以下是错误的猜想....
---------------------------------
个人猜想
为什么设置了虚析构函数就会去掉合成的移动操作
 虚析构的本意是通过基类指针释放派生类成员
也就是说现在的派生类中存在手动释放的成员或者 是new出来的  不在栈上而是在堆上长期驻留
假设基类合成了移动操作   基类->子类的过程  这个是没有问题的
子类->基类  这个问题就严重了   子类的基类部分被挖空的  但是它自身的部分还在  而移动操作过后是不允许操作原对象的
子类的一部分成员就没有释放一直在堆上

基类和子类的转化  无论是谁转谁 调用的都是基类的函数进行转化

30
如果派生类的拷贝移动没有显示调用基类的拷贝移动  则派生类的基类会使用默认初始化
赋值就很奇怪
需要在类内使用 Base::operator=(rhs)

31 构造函数和析构函数中调用虚函数  只会调用构造或析构所属类的函数  基类的构造和析构只会调用基类的虚函数  不会产生动态绑定

32  尾置返回类型
它出现在参数列表之后,所以可以使用形参
神奇
-----------------------------------------------------------------------------

33  string.c_str() 和string.data的唯一区别就是是否带const  如果使用强制类型的转化  c_str()依然可以变化


C++98	完全支持	是GCC 6.1之前版本的默认模式	-std=c++98 or -std=gnu++98
C++11	完全支持	从GCC4.8.1版本开始完全支持	-std=c++11 or -std=gnu++11
C++14	完全支持	从GCC 6.1版本开始完全支持，是GCC 6.1到GCC 10 (包括) 的默认模式	-std=c++14 or -std=gnu++14
C++17	完全支持	从GCC 5版本开始，到GCC 7版本，已基本完全支持。 是GCC 11版本的默认模式	-std=c++17 or -std=gnu++17
C++20	未完全支持	从GCC 8版本开始陆续支持C++20特性	-std=c++20 or -std=gnu++20 （GCC9及以前使用-std=c++2a）
C++23	未完全支持（标准还在发展中）	从GCC 11版本开始支持C++23特性	-std=c++2b or -std=gnu++2b

34  学习学习 类的内存结构

35 两个类互为友元  在class的定义没有完整出现时 不能调用class内部的函数  成员
所以要把类里面的函数如果调用的友元类的函数或者变量  那么这个函数的实现必须在 那个友元类之后(简单来说就是类实现分离)


36.子类公有继承之后.原本父类的保护成员  会变成子类的public

37,explicit仅对一个参数的构造函数有效


