---
title: "三次样条插值（Cubic Spline Interpolation）"
date:
  created: 2025-06-28
  updated: 2026-06-28
slug: Cubic Spline
categories:
  - math
tags:
  - math
description: "样条插值"
draft: true
---


# 三次样条插值（Cubic Spline Interpolation）

**三次样条插值**是一种常用的数值插值方法，通过在每对相邻数据点 \([x_i, x_{i+1}]\) 上使用一条独立的三次多项式，并在段与段的连接处施加适当的连续性条件，使整个插值曲线在函数值、一阶导数及二阶导数上保持连续，从而得到一条光滑的插值函数。其核心特点和定义可概括如下：

<!-- more -->

1. **分段三次多项式**  
   给定已排序的节点
   \[
     x_0 < x_1 < \cdots < x_n
   \]
   以及对应的函数值
   \[
     y_0, y_1, \dots, y_n,
   \]
   在每个区间 \([x_i, x_{i+1}]\) 上，三次样条插值函数 \( S_i(x) \) 为一条三次多项式：
   \[
     S_i(x) = a_i + b_i(x - x_i) + c_i(x - x_i)^2 + d_i(x - x_i)^3,
   \]
   其中 \(a_i, b_i, c_i, d_i\) 是在第 \(i\) 个区间内待定的系数。

2. **插值条件**  
   每段三次样条都必须满足：
   \[
     S_i(x_i) = y_i, \quad S_i(x_{i+1}) = y_{i+1},
   \]
   这保证了插值函数在每个节点处精确通过给定的数据点。

3. **光滑性条件**  
   为了使整条插值曲线足够平滑，需要在所有内部节点 \((x_1, x_2, \dots, x_{n-1})\) 处施加如下连续性约束：
   - 一阶导数连续：  
     \[
       S_i'(x_{i+1}) = S_{i+1}'(x_{i+1}), \quad i = 0, 1, \dots, n-2,
     \]
   - 二阶导数连续：  
     \[
       S_i''(x_{i+1}) = S_{i+1}''(x_{i+1}), \quad i = 0, 1, \dots, n-2.
     \]

4. **边界条件**  
   通常还需要在最左端 \((x_0)\) 和最右端 \((x_n)\) 处给定额外的边界条件，以唯一地确定所有系数。常见的边界条件包括：
   - **自然边界（Natural Spline）**：端点处的二阶导数为零  
     \[
       S_0''(x_0) = 0, \quad S_{n-1}''(x_n) = 0.
     \]
   - **固定端点导数（Clamped Spline）**：端点处的一阶导数已知  
     \[
       S_0'(x_0) = f'(x_0), \quad S_{n-1}'(x_n) = f'(x_n).
     \]
   - 其他形式的条件，如三阶导数或周期性等，视具体应用而定。

5. **线性方程组的求解**  
   根据以上条件，可以建立一个关于所有 \( c_i \) 的三对角线性方程组，通过数值方法（如 Thomas 算法）求解即可得到每个区间的三次多项式系数。

6. **优点**  
   - **平滑性**：一阶和二阶导数在节点处连续，保证了曲线平滑；  
   - **数值稳定**：只需在局部区间内使用三次多项式，避免了全局高阶多项式插值时易出现的数值不稳定性；  
   - **计算效率高**：构造的系数矩阵通常是三对角矩阵，能高效解算；  
   - **广泛应用**：在工程、计算机图形学、路径规划、数据可视化等领域被广泛使用。

7. **物理含义**  
   若将插值函数视为随时间变化的位移，则一阶导数代表速度，二阶导数代表加速度。三次样条的二阶导数连续性意味着在物理系统中，加速度在连接点处不会突变，保证了运动过程的平稳，因而在机械设计、机器人运动规划等场景具有重要意义。

综上所述，**三次样条插值**能够在较低的多项式阶数下，实现插值曲线在值与导数层面的平滑性，被广泛视为平滑插值的一种标准而高效的实现方式。



3.扩展
为什么三次样条插值广泛使用
但是二次和四次的不多见

为什么不用二次
三次样条插值可以满足二阶导函数连续
二次的无法满足,插值曲线在连接处可能出现曲率突变，视觉或物理上都显得不够光滑
速度是距离和时间的一阶导
加速度是距离和时间的二阶导
三次样条插值广泛使用于工业界,二阶导函数连续暗含了加速度连续这个物理概念

为什么不用四次
四次往往需要更大规模的线性方程组，而且高阶多项式本身数值上更容易出现不稳定或不必要的波动
三次样条所需解的系数矩阵通常为三对角矩阵，可用专门的三对角矩阵算法（如Thomas算法）高效且稳定地求解。
计算更加复杂而且效果不一定比三阶好

# 三次样条求解系数的方法

在三次样条插值（Cubic Spline Interpolation）中，我们需要在每个区间 \([x_i, x_{i+1}]\) 上构建一条三次多项式，使其既能够插值给定的数据点，又能够在区间连接处保证足够的光滑性（连续的一阶、二阶导数）。该过程的关键在于**求解三次样条的系数**。以下分步骤说明其具体方法与推导过程。

---

## 1. 问题描述

给定一组按升序排列的节点
\[
x_0 < x_1 < \cdots < x_n
\]
以及对应的函数值
\[
y_0, \, y_1, \, \dots, \, y_n,
\]
我们希望找到一条分段三次样条函数 \(S(x)\)，使得在每个区间 \([x_i, x_{i+1}]\) 上有：
\[
S_i(x) \;=\; a_i + b_i(x - x_i) + c_i(x - x_i)^2 + d_i(x - x_i)^3,
\]
并满足以下插值和光滑性条件：

1. **插值条件**  
   \[
   \begin{cases}
   S_i(x_i) = y_i \\
   S_i(x_{i+1}) = y_{i+1}
   \end{cases}
   \quad (i = 0, 1, \dots, n-1)
   \]

2. **一阶导数连续**  
   \[
   S_i'(x_{i+1}) \;=\; S_{i+1}'(x_{i+1}), 
   \quad (i = 0, 1, \dots, n-2)
   \]

3. **二阶导数连续**  
   \[
   S_i''(x_{i+1}) \;=\; S_{i+1}''(x_{i+1}),
   \quad (i = 0, 1
   , \dots, n-2)
   \]

4. **边界条件**  
   常用的“自然边界条件”规定：
   \[
   S_0''(x_0) = 0, 
   \quad S_{n-1}''(x_n) = 0.
   \]
   也可根据需要使用其他形式的边界条件（如固定导数、周期性等）。

---

## 2. 符号准备

令
$$
h_i = x_{i+1} - x_i, 
\quad (i = 0, 1, \dots, n-1),
$$
表示相邻节点的距离。  
我们把待求的三次样条系数分为：
\[
a_i,\, b_i,\, c_i,\, d_i, 
\quad (i = 0, 1, \dots, n-1).
\]

其中通常令
\[
a_i = y_i.
\]
这样可以使得在 \(x_i\) 处插值条件简单化：  
\[
S_i(x_i) = a_i + b_i(0) + c_i(0)^2 + d_i(0)^3 = y_i.
\]

---

## 3. 重点放在求解 \(c_i\)

在很多教材和实现中，会先把求解 \(b_i\) 和 \(d_i\) 的工作放在后面，先集中处理 \(c_i\)（对应着二阶导数信息）。因为如果能找到每段的 \(c_i\)，再用插值条件和导数连续性，即可顺带求出 \(b_i\) 和 \(d_i\)。

### 3.1 构建方程组

1. **插值条件**  
   \[
   S_i(x_i) = a_i = y_i, 
   \quad
   S_i(x_{i+1}) = a_i + b_i h_i + c_i h_i^2 + d_i h_i^3 = y_{i+1}.
   \]  
   （但多数实现中会直接用这些条件来辅助求出 \(b_i\) 和 \(d_i\)。）

2. **一阶导数连续**  
   \[
   S_i'(x) = b_i + 2 c_i (x - x_i) + 3 d_i (x - x_i)^2.
   \]
   \[
   S_i'(x_{i+1}) = b_i + 2 c_i h_i + 3 d_i h_i^2, 
   \quad
   S_{i+1}'(x_{i+1}) = b_{i+1}.
   \]
   因此  
   \[
   b_i + 2 c_i h_i + 3 d_i h_i^2 = b_{i+1}.
   \]

3. **二阶导数连续**  
   \[
   S_i''(x) = 2 c_i + 6 d_i (x - x_i).
   \]
   \[
   S_i''(x_{i+1}) = 2 c_i + 6 d_i h_i, 
   \quad
   S_{i+1}''(x_{i+1}) = 2 c_{i+1}.
   \]
   因此  
   \[
   2 c_i + 6 d_i h_i = 2 c_{i+1},
   \quad \text{即} \quad
   c_{i+1} - c_i = 3 d_i h_i.
   \]

4. **边界条件**  
   - 自然边界：
     \[
     S_0''(x_0) = 2 c_0 = 0 \;\Rightarrow\; c_0 = 0,
     \quad
     S_{n-1}''(x_n) = 2 c_{n} = 0 \;\Rightarrow\; c_n = 0.
     \]

**实际求解时**，往往使用**二阶导数连续**与**插值差分**形式，推导出对 \(c_i\) 的三对角方程组。下面以常见的推导为例。

---

### 3.2 三对角方程组（关键推导）

令
\[
\Delta_i = \frac{y_{i+1} - y_i}{h_i}, 
\quad (i=0,1,\dots,n-1),
\]
表示分段斜率（差商）。由曲线在内节点处的一阶和二阶导数连续的条件，可推导得到：
\[
h_{i-1}\,c_{i-1} + 2\,(h_{i-1}+h_i)\,c_i + h_i\,c_{i+1}
= 3\,\bigl(\Delta_i - \Delta_{i-1}\bigr),
\quad i=1,\dots,n-1.
\]
再加上自然边界条件：
\[
c_0 = 0, 
\quad c_n = 0,
\]
即可形成一个大小为 \((n+1)\times(n+1)\) 的线性方程组（其中有些行用于边界约束）。该方程组在典型实现中可简化成一个三对角矩阵，方便快速求解。

---

## 4. 由 \(c_i\) 得到 \(b_i\) 和 \(d_i\)

当 \(c\) 向量求得后，就能根据下面公式计算各段区间的 \(b_i\) 与 \(d_i\)。常见形式为：

\[
d_i \;=\; \frac{c_{i+1} - c_i}{3\,h_i},
\]
\[
b_i \;=\; \Delta_i \;-\; \frac{h_i}{3}\bigl(2c_i + c_{i+1}\bigr).
\]
也可在推导中出现细节略有差异，但本质相同。

最后，\(a_i = y_i\)（也有人在计算中令 \(a_i\) 为较复杂形式，但通常取 \(a_i = y_i\) 最方便）。

---

## 5. 自然边界示例的矩阵形式

以自然边界为例，方程组的矩阵通常表现为三对角形式，如下所示（示意）：



$
\begin{pmatrix}
1 & 0 & 0 & \cdots & 0 \\
h_0 & 2(h_0 + h_1) & h_1 & \cdots & 0 \\
0 & h_1 & 2(h_1 + h_2) & \cdots & 0 \\
\vdots & & & \ddots & h_{n-2} \\
0 & \cdots & 0 & h_{n-2} & 2(h_{n-2} + h_{n-1}) \\
0 & \cdots & 0 & 0 & 1
\end{pmatrix}
\cdot
\begin{pmatrix}
c_0\\
c_1\\
c_2\\
\vdots\\
c_n
\end{pmatrix}
=
\begin{pmatrix}
0\\
3(\Delta_1 - \Delta_0)\\
3(\Delta_2 - \Delta_1)\\
\vdots\\
3(\Delta_{n-1} - \Delta_{n-2})\\
0
\end{pmatrix}.
$




其中，首行与末行对应 \(c_0=0\) 和 \(c_n=0\)。


---

## 6. 小结与实现流程

1. **预计算**区间长度 \(h_i\) 和差商 \(\Delta_i\)。  
2. **构建三对角方程组**：
   \[
   A \cdot \mathbf{c} \;=\; \mathbf{B}.
   \]
   其中 \(\mathbf{c} = [c_0, c_1, \dots, c_n]^T\)。  
3. **求解 \(\mathbf{c}\)**：使用三对角矩阵解法或通用线性代数方法（如高斯消元、LU 分解等）。  
4. **计算 \(b_i\) 与 \(d_i\)**：通过已知的 \(c_i\) 和差商 \(\Delta_i\)，使用公式
   \[
   d_i = \frac{c_{i+1} - c_i}{3\,h_i}, 
   \quad
   b_i = \Delta_i - \frac{h_i}{3}(2c_i + c_{i+1}).
   \]
5. **得到每段三次多项式**：
   \[
   S_i(x) = a_i + b_i(x - x_i) + c_i(x - x_i)^2 + d_i(x - x_i)^3,
   \quad
   a_i = y_i.
   \]
6. **插值查询**：给定任意 \(x\) 值，先确定其所在区间 \( [x_i, x_{i+1}] \)，再带入对应的三次多项式即得插值值。

---

为了向0背景知识的人解释此概念,需要对所有的词语进行解释
三次样条插值
另一种定义:"强调连续性条件的分段三次多项式插值"
1.概念解释
什么是插值 Interpolation
是一种通过已知的、离散的数据点，在范围内推求新数据点的过程或方法

什么是样条
在数学学科数值分析中，样条（spline）是一种特殊的函数，由多项式分段定义。
将整个插值区间分成若干子区间（分段），在每个子区间上用一个多项式（通常是同阶的多项式）来进行插值，同时在各子区间的连接点（节点）处满足一定的连续性（如函数值连续、一阶导数连续、二阶导数连续等）

什么是三次?
三次多项式


分段的好处可以避免高次多项式的龙格现象
样条包含了分段多项式的概念,并且强调连接点处的连续性

2.定义
